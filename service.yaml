apiVersion: v1
kind: Service

metadata:
  name: corporate-website-service

spec:
  selector:
    app: corporate-website

  type: NodePort

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
